import 'package:flutter/material.dart';

class MetricsConverter extends StatelessWidget {
  static const routeName = '/metrics-converter';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // drawer: MenuDrawer(),
      appBar: AppBar(
        title: Text('Metrics converter'),
      ),
      body: Text('converter coming soon'),
    );
  }
}
